<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label "Name:" %><br />
    <%= f.text_field f.name %>
  </div>
  <div class="field">
    <%= f.label "Own3d.tv Game ID:" %><br />
    <%= f.text_field f.owned_game_id %>
  </div>
  <div class="field">
    <%= f.label "Twitch.tv Game ID:" %><br />
    <%= f.text_field f.twitch_game_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
